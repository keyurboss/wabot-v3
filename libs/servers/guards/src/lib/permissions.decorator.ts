// import { SetMetadata } from '@nestjs/common';

// export const RequiredPermission = (s: string) => SetMetadata('permissions', s);
// export const ApiPermissionNotRequired = () => SetMetadata('roles', null);
